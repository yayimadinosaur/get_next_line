ft_strsub
char		*ft_strsub(char const *s, unsigned int start, size_t len);
//copies str from position start - unsigned int

ft_memccpy
//copies until char c (\n)
//skips the \n and returns the index after

ft_memalloc
//makes all of it \0

ft_strdup
//mallocs + copies

ft_strchr
//searches for int (can be new line ascii)

ft_strcmp
//compares the strings if they chars are the same byte by byte


